# Built images
*.sif
# Extracted (no-sudo) apptainer binary
apptainer/bin-*/
# No-sudo rclone binary
bin/
# Offline bundle blobs (keep packages/README.md only)
packages/deb/
packages/sif/
packages/*.deb
packages/*.zip
# TLS cert + key (self-signed or corp — never commit)
tls/
# Runtime state / logs / generated config
data/
nginx/hwax.conf
# Infra env (commit only .env.example)
.env

# /data 이관 브리지(심링크) 이름 — dir/ 패턴은 심링크를 무시하지 않아 git stash -u 가 치운다(data-migration D0)
/data
