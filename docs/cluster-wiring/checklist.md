# 클러스터 배선 — 체크리스트

> 계획은 `PLAN.md`, 판단 근거는 `context-notes.md`. **dev 검증 / 실박스 검증**을 항목마다 갈라 적는다.

## 0. 전제 확인 (구현 전에 닫아야 하는 것)

- [x] **:5013 정본** — **KooSlurm**(`KooSlurmInstallAutomationRefactory/dashboard/smarttwin_mcp`). `~/claude/SmartTwinMCP` 는 고치지 않는다
- [x] **같은 경로가 양쪽에서 보인다**(사용자 확정) → ③ 은 경로 잇기
- [ ] 착지 경로 **실값** 확인 (실박스) — 다만 코드는 경로를 박지 않으므로 실값과 무관하게 성립해야 한다
- [ ] 권한 — stcx 쪽 실행 계정이 ste 잡 폴더를 읽을 수 있나 (실박스, 추측 말고 시도)
- [ ] 권한 — ste 계정이 만든 잡 디렉터리를 stcx 계정이 읽고/쓸 수 있는가 (실박스)
- [ ] ste 의 결과 보관 만료가 그 디렉터리를 지워 버리지 않는가
- [ ] DynaForge 컨테이너에 ssh/scp/rsync 가 있는가, ssh 별칭이 닿는가
- [ ] ste 헤드노드에서 포털 JWKS 를 얻을 수 있는가 (에어갭)

## ① 표기 규율 — ste / stcx

- [ ] 세 리포 전수 조사 결과에서 **확실히 틀린 것만** 추려 고친다 (모호·ssh 별칭은 손대지 않는다)
- [ ] 가드 테스트 — 위반 목록이 **줄어들기만 하는** 사전 패턴 (`test_box_paths.py` 선례)
- [ ] 규율 정본 한 자리에 선언 (리포 CLAUDE.md 또는 docs)
- [ ] 파일 이름에 든 것(`ste-stcx.conf.snippet` 등)은 **참조를 전부 찾은 뒤에만** 건드린다

## ② ste 업로드 스트리밍화

- [ ] `routes/jobs.py submit` — 상한을 **읽기 전에** 본다
- [ ] `jobs.py create` 가 바이트가 아니라 경로를 받게 (또는 스풀 파일을 그대로 옮기게)
- [ ] 되감기(실패 시 `shutil.rmtree`) 가 여전히 성립하는지
- [ ] 시험 — 상한 초과가 413 이고, 큰 파일에서 RSS 가 파일 크기에 비례하지 않는다
- [ ] 두 앞단 모두에 `client_max_body_size` + `proxy_request_buffering off` (포털 conf, stcx nginx 스니펫)

## ③ stcx 반입 자동화

- [ ] 공유 NFS 가 확인되면 — ste 잡 디렉터리 경로를 `work_dir` 로 잇는다 (전송 계층 없음)
- [ ] 확인 안 되면 — 반입 수단을 정하고 그 경로만 자동화한다
- [ ] 시험 — 경로 규약이 코드로 못 박히고, 어긋나면 **조용히가 아니라 명시적으로** 실패한다

## ④ cae00→stcx ssh 왕복

- [ ] 실패 모드 분리 — 호스트 없음 / 인증서 만료 / 비0 종료 / 타임아웃이 반환값에서 구분된다
- [ ] 낙하 시뮬 계열의 **별도 ssh 경로**가 코드 어디인지 확정 (없으면 그것이 결손)
- [ ] 게이트웨이 타임아웃보다 오래 걸리는 명령의 처리 규약
- [ ] 시험 — 가짜 ssh 하네스로 네 갈래 (dev 검증 가능)

## ㉮ stcx 잡 → DynaForge 작업 옵션

- [ ] 길 선택 — SmartTwinMCP 도구 재사용 vs 직접 ssh (근거를 노트에)
- [ ] DynaForge 작업 레지스트리에 추가 (정의·핸들러·폼·MCP 노출)
- [ ] 오래 걸리는 외부 잡의 상태 폴링 — 누가 주체인가
- [ ] 산출물을 `session_files` 로 되먹임
- [ ] 시험 — 작업 목록에 뜨고, 가짜 백엔드로 제출→상태→회수가 돈다

## ㉯ ste → MCP → 포털

- [ ] ste MCP 서버 골격 (도구: 앱 목록·제출·상태·로그·결과)
- [ ] 게이트웨이 등록 (설정 또는 매니페스트 자동 발견)
- [ ] 대용량 업로드는 MCP 도구로 나르지 않는다 — 지침/경로 방식
- [ ] 시험 — 게이트웨이 `tools/list` 에 뜬다

## ㉰ 포털 로그인 → ste 계정·PAT 자동 연결

- [ ] 발급·보관 주체 결정 (포털 / ste / 게이트웨이)
- [ ] ste 승인제 처리 — 포털 로그인이 갈음하는가, 신원만 잇는가
- [ ] "SSO 리다이렉트 없이 401" 요구와 충돌하지 않는 설계
- [ ] 에어갭 JWKS 대책
- [ ] 잡 소유자가 **본인**으로 남는지 (서비스 PAT 1개 귀속 버그를 되풀이하지 않는다)
- [ ] 시험 — 신원이 포털→게이트웨이→ste 까지 끊기지 않는다

## 사용자 몫 (실박스)

- [ ] cae00 에서 `routes.local.env` 의 `ste=` 가 ste 헤드노드를 가리키는지 확인
- [ ] ste·stcx 공유 NFS·권한 실측
- [ ] 실주행 E2E — 파일 업로드부터 stcx 낙하 결과 회수까지
