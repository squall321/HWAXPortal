# 심의 UX 7건 — 체크리스트

승인된 순서대로. 체크는 **실제로 확인한 것**만 한다.

## ① 화이트리스트 네 칸 — 엔진에 있는데 웹에서 죽은 것을 켠다

- [x] 백엔드 `DelibOpts` 에 `non_negotiables` 선언(backend/app/agent/routes.py)
- [x] 백엔드 `DelibOpts` 에 `stop_after_round` 선언
- [x] 백엔드 `DelibOpts` 에 `rounds_so_far` 선언
- [x] 백엔드 `DelibOpts` 에 `append_to_report_id` 선언
- [x] `delibOptsToWire`(chatStore.ts)에 `stop_after_round` 추가 — 패널 토글은 이 겹도 통과해야 한다
- [x] `continueDeliberation` 이 `rounds_so_far`·`append_to_report_id` 를 실어 보낸다
      (`rounds_so_far` 는 `totalRounds`(계획값)가 아니라 **발언의 최대 round**(실측값)로 센다 —
      체크포인트로 일찍 멈추면 계획값이 과대평가된다)
- [x] ⚠ 체크포인트 토글이 이어하기에 따라붙지 않게 `stop_after_round: 0` 을 명시 —
      `extraDelibOpts` 가 패널 토글보다 **뒤에 병합**되므로 1회성이 보장된다
- [x] 회귀 방지 테스트 4건 — 프론트 두 경로에서 키를 뽑아 모델 필드와 집합 대조.
      **일부러 필드를 빼서 실제로 실패하는지 확인**했다(추출 실패 시에도 실패하게 만들었다)
- [x] 모델 통과 실측 — 네 값 전부 `model_dump` 를 통과해 중계된다
- [x] **실제 심의를 돌렸다**(dev, 2026-09-11 — 새 심의 1회 + 같은 보고서 이어하기 1회, context-notes D-17).
      보고서 이어붙이기 ✓(RA #60 에 페이지 추가), 양보 불가 조항은 모든 좌석 base 에 실림 ✓(결정적 확인),
      교차심문 2표적 ✓(4석 8건·6석 12건), voc off/auto ✓, 재심사 신규 2석 ✓.
      ⚠ **라운드 번호는 웹에서 여전히 1R 이었다** — 엔진의 display_round 를 포털·프론트가 안 썼다. 고쳤다(a3fd46d).
      ⚠ dev RA 에 검증용 보고서 #60('[E2E 검증용 테스트 심의] …')이 남아 있다 — 필요 없으면 지워도 된다

## ② 반박 관계를 구조로 남긴다 (⑥·⑦의 재료)

- [x] 엔진이 `rebut` 를 turn 이벤트에 구조로 싣는다(`_rebut_items`, 테스트 3건)
- [x] 프론트 타입 `DelibTurn.rebut` + `mergeDelib` 병합
- [x] 심의 종료 후 **관계도** 렌더(`DelibGraph.tsx`, mermaid) — 누가 누구를 반박했는지.
      임시 진입점으로 확인: 노드 4·엣지 4 렌더, 좌석 색이 회의록과 일치,
      **반박이 없으면 아무것도 안 그린다**, mermaid 파싱 오류 0
- [x] `chatStore` 저장 트림에 `rebut` 예산 — 관계만 남기고 본문은 자른다(원문은 say 에 있다).
      쿼터를 넘기면 저장이 대화 절반을 버리고 재시도하므로 두 번 싣지 않는다
- [x] 서버 대화 저장소가 stance·non_negotiable·rebut 를 `meta` 로 영속(스키마 변경 없음) +
      프론트 복원. 구 저장분은 meta 가 없어 그대로 비어 온다(깨지지 않는다)
- [x] 이어하기가 **미해결 쟁점**을 승계 — `continue_summary` 의 남는 예산에 붙인다.
      결정문을 밀어내지 않고, 뒤 라운드(더 좁혀진 쟁점)를 먼저 싣는다.
      경계 실측: 예산 205/300/600/1200 전부 준수, 항목이 하나도 안 들어가면 빈 문자열
- [x] JS 정본(infra/pipeline/hwax-deliberate.js)도 같은 구조 — R2 스키마 `rebut` 를 객체로.
      rebut 를 읽는 곳 셋(summarize·readable, hwax-risk-review, save-delib-conversation)이 객체를
      문자열화하면 `[object Object]`·dict 표기가 박혀서 전부 고쳤다. 구형 문자열 반박도 읽힌다
- [x] ⚠ 게이트웨이 `save_conversation` 이 meta 를 **조용히 버리고** 있었다(`_msg()` 가 4칸만 옮김) —
      MCP 심의의 반박이 포털 관계도에 닿을 길이 없었다. 웹 경로와 같은 자르기로 통과(테스트 2건,
      옛 게이트웨이에서 실패 확인)

## ③ 심의 좌석 조직도

- [x] 다중 선택 조직도 `SeatBrowser.tsx` — 선택 집합·토글·상한을 **전부 props 로**.
      `PersonaBrowser` 를 고치지 않고 새로 짰다(그건 ChatContext 단일선택에 박혀 있다)
- [x] 관련도를 조직도 카드에 배지로 머지 — 실측: 카메라 21석에 100%~87% 배지가 그대로 붙는다
- [x] 상한 12 를 조직도 안에서 보이게 — 헤더 `선정 12/12 — 가득`(호박색), 못 고르는 카드는
      흐리게 + `가득` 비활성(조용히 무시하지 않는다), 분야 트리에 `12석` 배지로 쏠림이 보인다
- [x] 하한 2 — **0석은 경고하지 않는다**(서버 자동 발굴이라 정상). 1석일 때만 경고한다
      (엔진이 2석 미만이면 `no_personas` 로 죽는다 — deliberation.py:2472)
- [x] 두 입구 모두에 배선 — `ExpertPicker`(/심의 경로)와 `HandoffBrief`(챗 핸드오프 경로)
- [x] HandoffBrief 의 어긋남 정리 — `low_confidence` 경고가 **없었다**(ExpertPicker 에만 있었다),
      좌석 상한이 **없었다**(`personas` 는 백엔드 `max_length=12` 라 13석이면 422)
- [x] 실 풀 781명·22분야로 임시 vite 진입점 검증 — 0/1/12석 상태, 검색 9건, 1000px 미만 레이아웃
- [x] `axes` 는 HandoffBrief 에만 있다 — **어긋남이 아니라 정당한 차이로 판정.** 서버 `_seat_axes` 는
      `if not history: return []` 이고(app.py), `/심의` 페이지는 이어 붙일 대화가 없다. ExpertPicker 에
      축 UI 를 붙이면 늘 빈 칸인 죽은 코드가 된다

## ③+ 좌석 확장 — "12석 상한이 꼭 필요한가" 에서 나온 갈래 (context-notes D-12·D-13)

- [x] 교차심문 기본 켜짐 + 표적을 관련도 상위 2명으로(용어 겹침 IDF, 조사 제거, 커버리지 보정)
- [x] 좌석 상한 12 → 20, 네 곳(엔진 `MAX_REQ_SEATS`·포털·ExpertPicker·HandoffBrief)을 계약 테스트로 묶음.
      엔진만 되돌려 실제로 실패하는 것 확인. 엔진이 잘라내면 이제 status 로 알린다
- [x] 좌석 프롬프트 상한 `DELIB_SEAT_CTX`(48K, 좌석마다 같은 몫) — **수렴 라운드는 교차심문과 무관하게
      전문을 줘서** 교차심문만으로는 N² 가 안 닫혔다. 실데이터 12라운드 중 상한 안 8개 바이트 동일
- [x] JS 정본에도 교차심문·좌석 상한 — 파이썬과 배정 162/162 일치

## 요청 3번 — 한 번 돌고 전문가 로스터 선택 (context-notes D-18)

- [x] '한 번 돌고' — 1라운드 체크포인트 토글(①에서 살림), 실주행으로 1라운드 뒤 멈춤 확인
- [x] ⚠ '로스터 선택' 절반이 **빠져 있었다** — 이어하기 칸이 의견만 받고 좌석은 자동 승계였다(2026-09-11
      "전부 다 진행했나" 질문에 대조하다 발견). 이어하기 🗂 좌석 조정(RosterEditor) — 빼기(경고)·조직도로 더하기
- [x] 명단을 고치면 `delib_opts.rescreen=0`(엔진 요청 단위 스위치 신설) — 두 겹 화이트리스트·계약 테스트 확인
- [x] 복원한 심의는 personas 가 비어 이어하면 좌석을 새로 발굴하던 것 — 발언자로 채움
- [x] 실제 요청 본문을 가로채 확인 — 고른 명단·rescreen 0·합류/제외 의견, 복원형은 발언자 4석

## ④ 도구 영역 분류 (context-notes D-14)

- [x] 정본 = 게이트웨이 추적 파일 `tool_areas.json`(config 아님 — gitignore·--force 재생성). mtime 캐시라
      고치면 재기동 없이 반영
- [x] 영역 13개 — 판정은 도구 지정 > 이름 패턴 > 앱 기본값. 실 462개 전부 분류, 오타·미정의 참조 0
- [x] 미분류를 숨기지 않는다 — `/tools-map` 의 `unclassified`, UI 는 점선 호박색 '미분류 N' 칩
- [x] 세 입구(도구 카드·심의 선정·시작 선택기)에 영역 칩 1단 — 영역을 고르면 앱 드롭다운도 좁혀진다.
      목록 상한에 걸리면 '… N개 더'
- [x] MCP 직결 사용자도 — `list_tool_apps(by='area')`, 권한 없는 앱 도구는 영역 보기에서 뺀다
- [x] 테스트 — JSON 중복 키(파이썬이 조용히 덮는다)를 일부러 넣어 잡는 것 확인

## ⑤ VOC 먼저 (context-notes D-15)

- [x] ⚠ 먼저 결함부터 — 기존 VOC 환기가 부정 VOC·제품별 이슈를 **0건**으로 보고 있었다(목록 도구의
      원소별 블록을 `_parse_json` 이 마지막 하나로 읽음). 수정 후 5건, 옛 엔진에서 실패하는 회귀 테스트
- [x] `delib_opts.voc` = auto(종전) | off | always — 두 겹 화이트리스트(포털 Literal·엔진) 모두
- [x] `POST /deliberate/voc-preview` — 화두에서 영어 검색어를 뽑아 search_voc(FTS·영어 권장). 검색어별
      교차·중복 제거, 못 씀·실패·없음 구분
- [x] 사람이 고르는 UI `VocFirstPanel` — 최대 6건, 고른 순서 유지, 두 입구 공통
- [x] 고른 것으로 화두 보강 → `human_note`. 고른 VOC → `evidence`(원문 그대로). 직접 봤으면 `voc:'off'`
- [x] 계약 테스트를 /심의 페이지까지 넓힘(종전엔 ChatContext 만 봤다)

## ⑥ 되묻기 (context-notes D-16)

- [x] Job 별 최소 정보 칸 — 원인 규명·해석 설계·구축·자유=대상·현상·조건·범위·이미 확인한 것,
      안 선택·신뢰 판정·시험 설계는 각자. 칸 키는 서버 고정(LLM 이 칸을 못 지어낸다)
- [x] 빈 칸을 한 번에 스캔(`POST /deliberate/clarify`) — AIDataHub `_scan_missing` 선례, 최대 4개
- [x] 답을 담을 자리 = **화두**의 `[보강 정보]` 블록 — 좌석 추천·매 라운드·회의록에 다 실린다.
      보강 후 자동 재스캔으로 남은 칸만 다시 묻는다('몇 번 더')
- [x] 후보가 하나면 고르게 하지 않고 입력칸 예시로만(D-7)
- [x] dev 실측으로 판정을 전부 코드로 옮김 — '있음'은 원문 대조, 칸별 파싱, 적용 여부, 한자 문구 거부
