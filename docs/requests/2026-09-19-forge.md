# 2026-09-19 포지 요청 — 항목별 장부

왜 이 문서가 있나 — 이 라운드의 요청(각 forge 동작 A~E + SignalForge 보고)은 **채팅으로만 왔고 어느
리포에도 남지 않았다.** 그래서 "다 처리됐나" 를 항목 단위로 대조할 방법이 없었다(9/19 전수 검증이
지적한 실제 누락이다). 요청 원문을 재구성해 **처분·근거·남은 것**을 한 장에 둔다. 다음 라운드부터는
요청이 오면 먼저 여기에 적고 시작한다.

기준 시각 2026-09-19. 근거의 `커밋`은 그 리포의 커밋이다.

## A — StepForge 검출·잡 러너 (요청: 간섭 오탐, 재개, 재질 선택자)

| # | 요청 | 처분 | 근거 | 남은 것 |
|---|---|---|---|---|
| A-1 | 간섭 6건이 가짜로 보인다 — 부울/스크린 구분이 없다 | 완료 | StepForge `771d799`·`823aca4` — `pen_method` 신호, 공차 수준 집계, **스침/묻힘 갈림**(D-369) | 행의 종류를 실제로 바꾸는 것은 사람 몫(E-1) |
| A-2 | 검출 샤드가 죽으면 목록이 조용히 빠진다 | 완료 | `771d799` — 두 진입점 모두 실패로 끝난다(D-363), 재현 시험 5건 | — |
| A-3 | 재개(resume) 체크포인트가 지워지는 시점 | 완료 | `771d799` — `ckpt.finish()` 를 커밋 뒤로, 무효화 조건을 안내에 명시(D-364) | 요청서의 "공차만 바꿨는데 결과가 같다" 는 가르는 법만 적고 단정 안 함 |
| A-4 | 재질로 접촉을 고르고 싶다 | 완료 | `771d799` — `between_materials`·`between_paths`, 오류 문구가 길을 알린다 | REST `/rules` 에는 도움말이 없다(웹 편집기는 예시를 따로 보여 준다 — 의도) |

**A-1 의 결정(D-369)** — 공차 아래 겹침을 자동으로 `touching` 으로 **내리지 않는다.** 두께만으로는
"맞닿을 면이 공차만큼 겹친 것"과 "얇은 부품이 통째로 묻힌 것"이 갈리지 않고, 뒤엣것을 내리면 진짜
관통이 조용히 사라진다. 대신 `contained_fraction`(겹침/작은 쪽 부피)과 `pen_over_min_extent` 를 내서
사람이 **알고** 고르게 한다. 라이브 실측: 얕은 겹침 0.002 → `touching_candidate`, 깊은 관통 0.40 →
`buried_suspect`.

## B — StepForge 접촉 규격

| # | 요청 | 처분 | 근거 |
|---|---|---|---|
| B-1 | 접촉 규칙 우선순위가 문서에 없다 | 완료 | `771d799` — SIDECAR_SPEC §3.1 "구체성이 아니라 **선언 순서**", options 상속, `skip` 으로 끊기 |
| B-2 | 도구에서도 보여야 한다 | 완료 | `get_part_rules` 가 `contacts_help` 를 함께 낸다(라이브 확인) |

## C — DynaForge(KooRemapper) 코드

요청은 그 리포 몫이라 **요청서로 전달**했다 — `KooRemapper` `e35a5ab`
(`platform/docs/REQUEST-deploy-fixes-20260919.md`, 브랜치 `feat/kooremapper-platform`).
결함 넷: 감독자가 멀쩡한 API 를 재기동(SIGPIPE 오판) · `rc=$?` 가 항상 0 · autostart 가 감독자를
안 띄움 · MCP 공개 주소를 요청 헤더로 지어냄.

**남은 것** — 검증 결과 그 넷은 **아직 코드로 안 고쳐졌다**. 그리고 DynaForge 쪽 수정 164건은
`integrate/defects-20260918` 에 있고 요청서가 기준선으로 지목한 브랜치에는 없다(병합 의도 확인 필요).

## D — 배포·게이트웨이 (HEAXHub)

| # | 요청 | 처분 | 근거 | 남은 것 |
|---|---|---|---|---|
| D-1 | 한글 이름 사용자가 앱에 못 들어간다 | 완료 | HEAXHub `57d1e68` — 헤더에 못 싣는 이름은 비워서 넘긴다, 회귀 시험 | cae00 반영 |
| D-2 | StepForge 공개 주소가 자리표시자로 나온다 | 완료(dev) | 호스트 `.env` 한 줄 + HEAXHub `e994243`(재배포 스크립트가 `.env` 를 스스로 싣는다) | **cae00 은 한 줄을 따로 넣어야 한다** — cae00 가이드 09-19 절 |
| D-3 | 406 라우트 | 설계대로 | 요청서에 이유를 적어 회신 | — |

**D-2 의 실사고** — 04:45 에 다른 세션이 재배포하자 그 값이 **조용히 빠진 채** 앱이 멀쩡히 떴다.
런처는 `os.environ` 의 `APPTAINERENV_*` 만 컨테이너로 넘기는데 스크립트가 `.env` 를 안 읽었다.
구조로 고쳤고(위 커밋) 무력화 3/3 으로 지킨다.

## SignalForge — 게이트웨이 백엔드 주소 드리프트

| 처분 | 근거 | 남은 것 |
|---|---|---|
| 완료 | 게이트웨이 `6fbb896`(주소를 **그 서비스의 `.env` 포트**로 결정) + 포털 `b9fb08c`(update-all 이 어긋난 주소를 잡는다) | cae00 에서 `update-all` 1회 — 8013→8008 로 스스로 고쳐진다 |

## E — 사용자 서버 작업 (dev 에서 할 수 없음)

| # | 요청 | 상태 |
|---|---|---|
| E-1 | 간섭 6건 정정 | **사용자 실행** — `inspect_report` 로 `sliver_verdict` 를 보고 `set_interface` 로 바꾼다 |
| E-2 | 중복 DV2 과제 삭제 | **사용자 확인 후 실행** — 삭제는 되돌릴 수 없어 지목을 받아야 한다 |
| E-3 | 메시 → 덱 재생성 | **사용자 실행** |

## 아직 열려 있는 것 (이 라운드가 끝나도 남는다)

1. **cae00 반영** — 위 09-19 절 그대로. SignalForge 자동 교정 + StepForge 공개 주소 한 줄.
2. **절차 면제 열쇠(W-100) 라이브 실측 1회** — MUST_GATE 도구가 든 절차를 한 번 완주시켜 감사원장에
   `purpose: procedure` 가 남는지 본다. 설계상 관측점이 거기 하나다. 게이트웨이 MCP 재연결이 선행.
3. **감사로그의 시험 산출물 약 247줄** — 지울지·표시할지·둘지는 사용자 결정. 결정 전까지 파괴 도구
   감사 통계가 오염돼 있다(`/data/svc/mcp-gateway/README-test-artifacts.md` 에 기준·건수).
4. **HEAXHub `materialtwin-release-to-drive.sh` 미커밋 변경(09-05, 소유 불명)** — 커밋할지 버릴지.
5. **DynaForge 브랜치 병합 여부와 요청서 결함 넷** — 그쪽 세션 몫.
