<!-- 설계 리스크 심사 — 단계별 실행 체크리스트(plan.md §9 에서 도출, 진행하며 체크) -->
# 설계 리스크 심사 — 체크리스트

정본은 `plan.md`(§9 단계 계획·§10 열린 질문). 이 문서는 실행 순서와 완료 판정만 요약한다. 항목 뒤 괄호는 plan.md 절. B 토폴로지(§0.7 #12) — 자산·러너·UI 는 HEAX 앱 `hwax_risk`(리포 `HWAXRisk`, `/apps/hwax_risk/*`, DB `$HEAX_DATA_DIR/risk_review.db`), 포털은 창(타일·`/risk`·NavLink)만. 아래 경로에서 `backend/`·`frontend/` 는 앱 리포 기준이고 포털 파일은 `포털:` 접두를 붙였다.

## 착수 전 — 사용자 결정 (§10, P0 착수 전 답이 필요한 것)
- [ ] (23) 매니페스트 확정값 — `id hwax_risk · name HWAX Risk Review · owner cae-automation · status beta · visibility company · resources{1, 2, false}` 승인(`team` 은 `seed_admin_org` 일치 확인 선행)
- [ ] (24) 리포 위치 — `source.url` GitHub `https://github.com/squall321/HWAXRisk.git`(ref main) 승인(`file://` 은 cae00 스캔 fetch 실패 반복)
- [ ] (28) 스캐폴드 관례 확정 — 이름은 실존 리포 `HWAXRisk`·env 접두 `HWAXRISK_`·`source.url` 로 확정(계획서 반영 완료). 레이아웃은 선택 — (i) P0 착수 시 `backend/`+`frontend/`+`fastapi_react` 로 이동(기본) / (ii) P0 는 루트 `app/`+`fastapi` 유지, P1 전환. 어느 쪽이든 §9.1 A-δ 코드 델타는 P0 즉시(§10.8 #28)
- [ ] (3) 러너 자격 — (a) 서비스 계정 포털 PAT `HWAXRISK_PORTAL_PAT` 발급(계정·groups·ttl 365·aud `mcp-gateway`) 승인 · (b) SettingsPage 사용자 PAT 등록 UX P0 포함 승인(대리 발급 없음) · (c) 서비스 자격 2키 분리 승인 — `HWAXRISK_PORTAL_PAT`(scopes read, 패널·소스 읽기)와 `HWAXRISK_PORTAL_PAT_RW`(scopes read+write, RA·ADH 쓰기 전용, §8.2.7)
- [ ] (25) 러너 엔진 호출 경로 — (A) 앱 → 포털 `/agent/chat` 정본, (B) `:9009` 직접은 `HWAXRISK_AGENT_URL` 설정 박스 폴백만(기본 꺼짐)
- [ ] (1) RA 시스템관리자 PAT — `bootstrap_ra_ontology.py --base <RA REST 오리진>` 실행자 env `RA_ADMIN_PAT` 1회 사용 승인(앱 런타임 무보유. 미승인이면 P0(2) 보류, `external_sync.ra=unavailable`)
- [ ] (10) AIDataHub doc_type 3종·`risk-review-memory` 생성·`external_source=hwax-risk`·`HWAXRISK_AIDH_API_KEY` 발급 주체·`AUTH_REQUIRED` 값 확인
- [ ] (16) 브라우저 신원 = 앱 `identity.py` 가 heax `/api/v1/auth/me` 되묻기(HEAX 무수정) — 기본값 그대로 갈지, `copy_identity` additive 를 원하는지
- [ ] (17) MCP 호출자·게이트웨이 헤더 — 읽기 범위는 `actor` 를 폐기하고 §8.2.5 caller 해석·`brief_token` 으로 확정(반영 완료). 남는 결정 둘 — ① 쓰기 귀속 `actor` 미검증(`actor_verified:false`) 유지 vs 게이트웨이 `x-hwax-user` 전달, ② 같은 자리에서 `x-hwax-scopes` 전달(§8.4.1 gateway.py 1행). 둘 다 기본 미채택
- [ ] (18) 앱 시크릿 `secrets.env`·`_user_credentials` 가 Drive tar 에 포함됨 — ①(확정) ttl ≤365 로 수용. DB·exports 는 §5.2.5 (3a) 로 `--exclude`+age 암호 사본이 정본이다 **기본값이 ②+③ 으로 바뀌었다** — `_user_credentials` 는 `portal_pat_enc`(Fernet, 키 `HWAXRISK_CRED_KEY`)로만 저장(③ 반영 완료)이고, 그 키가 같은 `secrets.env` 에 있으므로 `appdata-to-drive.sh` 에 `*/secrets.env` 제외 패턴을 넣는 ②(HEAX 스크립트 additive 1행) 승인이 남는다
- [ ] (18b) 데이터 등급 값 — `internal|confidential` 2값 유지(기본). 투영·MCP 경계는 등급이 아니라 `mcp_visibility` 가 정하도록 분리됨(반영 완료) — 남는 질문은 '`confidential` 이면 조직 공개를 막을지'
- [ ] (29) 과제 공유 기본값 — 명시 초대만(기본) vs 부서 자동 viewer / 열람(GET) 감사 로그 남길지(기본 안 남김)
- [ ] (30) 퇴사자 자산 — 수동 이양(기본, `risk_admin_roles`) vs 자동 이양 (P4 이후 운영)
- [ ] (31) 사람 finding 을 선례로 되먹일지 — `risk_prior_include_human` 기본 true (P5 착수)
- [ ] (32) 인젝션 위생 강도 — 어휘 적중 시 자리표시자 차단(기본) vs 어휘 축소(X01~X06) vs 경고만 (P1 착수)
- [ ] (14d) 사전 메이저 승급 뒤 `recompute_part_keys.py` — 수동 실행+경고 배너(기본) vs 기동 시 자동 vs 승급 API 동기 실행 (P2 착수)
- [ ] (33) 기각·반증 선례(E5−)를 다른 과제 브리프에 실을지 — 실음(기본, 블록 분리·6줄 상한) vs 계보 안에서만 vs 안 실음 (P5 착수)
- [ ] (34) `rr_panel_calls.result_gz` 보존 — 영구(기본) vs 인용된 것만 영구 vs 항목 상한 (P4 이후 운영)
- [ ] (19) HEAX 콜백 `next` allowlist additive(2단 → 1단) 채택 여부 — 기본 미채택
- [ ] (20) app-data 백업 — dev crontab `appdata-to-drive.sh` 일 1회 시각(기본 03:30), RETAIN 5
- [ ] (2) 게이트웨이 `rest.heax` — 기본 불필요(heax 서비스 PAT 직접 읽기). 원하면 설정 3곳 변경(P1 착수 전 선택)
- [ ] (8) 성격 통제 어휘 v1(8축 — `char:constraint` 포함)·택소노미 v1 초안 확정 주체 지정(초안 그대로 시작 가능)
- [ ] (36) 요구(`rr_requirements`) 입력 주체와 `standard` 규격 원천 — owner·editor 등록·`source_ref` 자유 문자열(기본), 요구 미등록 과제도 심사 허용 (P1 착수)
- [ ] (37) 소스 응답 계약 위반 강도 — `risk_source_drift_block=false`(표기만)+`caveat='parser_differs'`(기본) vs 차단 (P1 착수)
- [ ] (38) 과제↔제품 대응표 유지 주체 — 등록 화면에서 owner 다중 선택(기본). 비면 라벨 경로 1·2 자동 확정 0·경로 4 미실행 (P1 착수)
- [ ] (39) 모델 상한 — `risk_max_leaf` 1500·`risk_max_interfaces` 6000·예산 180 s(`allow_large` 600) 값 승인, 초과는 409 후 명시 승인(기본) (P1 착수)
- [ ] (35) MCAD 없는 과제(K파일만·ECAD만) 심사 범위 — 대표 1석+`risk_mcad_domains` deferred(기본), 등록부·선례는 분리하지 않음 (P2 착수)
- [ ] (40) `delib_opts.evidence` 항목 상한 12 상향 여부 — 접어 둔다(E10 을 E5 항목의 세 번째 블록으로, 엔진 무수정) (P5 착수)
- [ ] (41) material 좌석의 자리 — `heax-materialtwin_web` backend_down 실측(`_MATERIAL_TOOLS` 게이트웨이 0건). ① 가용성으로 좌석 `deferred`+P0 (5) material 항 조건부 skip(기본) vs ② 로스터 도메인 15→14 vs ③ 필수 호출을 `material_usage` 로 교체 (P0 착수 전)

## P0 부트스트랩 — 계약·엔진 손잡이·저장소 준비 (§9.1)
산출물 A — 앱 리포 `HWAXRisk` 스캐폴드(ThermalShockMCP 골격 복제 + `fastapi_react` 레이아웃, 파일 첫 줄 한국어 주석). `[x]` = 파일이 실존 스캐폴드(`/home/koopark/claude/HWAXRisk`, 2026-08-31 커밋 0건)에 이미 있음 — 계획값과의 차이는 바로 아래 '델타' 줄이고 델타가 닫혀야 항목 완료(§9.1 A-δ, §10.8 #28)
- [x] `.portal/manifest.yaml` — §8.2.2 확정값 전문(schema_version 2 · mcp{expose true, path /mcp, streamable_http, allowed_groups []} · launch{service, /api/health, on_failure 3, env{HWAXRISK_DATA_DIR:/data, PYTHONNOUSERSITE:"1"}}) — 실존
  - [ ] 델타: health `/health`→`/api/health` · `build.stack fastapi`→`fastapi_react`(#28 (i)) · visibility `team`→`company` · memory_gb 1→2 · `launch.env` 추가 · `mcp.allowed_groups: []` 추가 · description 의 `/api/v1`·`/health`·`hwax_risk.db`·'도구 3종' 문구 갱신
- [x] `backend/pyproject.toml`(fastapi · uvicorn[standard] · 'mcp>=1.10,<2' · pydantic · httpx, packages.find include app*, version 0.1.0) — 실존 루트 `pyproject.toml`(jsonschema·pyyaml 포함)
  - [ ] 델타: `backend/` 로 이동(#28 (i)) · `httpx` 를 runtime deps 로 · package-data 에 `assets/*.json`
- [x] `backend/app/main.py` — ① `/api/*` ② `app.mount('/mcp', FastMCP(streamable_http_path='/').streamable_http_app())` ③ 마지막 `app.mount('/', StaticFiles(../../frontend/dist, html=True))`, lifespan §8.2.10 ①~⑦(`mount('/', mcp_app)` 금지) — 실존 `app/main.py`
  - [ ] 델타: `mount('/', mcp_app)` 제거 → `/mcp` 서브마운트 + 마지막 `/` StaticFiles((ii) 면 `app/static/`) · `GET /health`→`GET /api/health {ok, app_version, schema_version}` · lifespan ③④⑤⑦ 추가
- [x] `backend/app/config.py` — 데이터 루트 `HWAXRISK_DATA_DIR > HEAX_DATA_DIR > <리포>/data`, `mkdir -p`+`W_OK`, Settings §8.2.6(env 접두 `HWAXRISK_`), `secrets.env`(0600) 로드 — 실존 `app/config.py`(우선순위·W_OK 기동 중단 구현됨)
  - [ ] 델타: `DB_FILENAME` `hwax_risk.db`→`risk_review.db` · Settings §8.2.6 전 필드 · `secrets.env` 로드
- [x] `backend/app/identity.py` — `identity.current(request) -> Identity{email, display_name, role, organization, anonymous, source}`, Bearer → 쿠키 `heax_access_token` → heax `/api/v1/auth/me`, sha256 키 TTL 60 s 캐시, `X-Heax-User-*` 미사용 — 실존 `app/identity.py`(`X-Heax-User-Email` 을 `header_unverified` 로 읽는 방식 — §8.2.8 위반)
  - [ ] 델타: 헤더 읽기 제거 → `identity.current` `/auth/me` 되묻기 + TTL 60 s 캐시, `api.py` 호출부 `Depends(identity.current)`
- [x] `backend/app/risk_store.py` — `MIGRATIONS` v1 = §5.2.2 DDL 전문(rr_* 41표) + 살림 표 `_schema_migrations`·`_user_credentials`, `PRAGMA user_version`·WAL, `pre-migrate-<ts>` 사본 — 실존 `app/risk_store.py`(v1 = 4표, 이력 표 `schema_migrations`)
  - [ ] 델타: `schema_migrations`→`_schema_migrations(+app_version)` · v1 41표 전문(`rr_panels.model_json`·`brief_gz`·`rr_coverage.model`·소유·수명주기·사람 개입 4표 `rr_project_members`·`rr_gate_acks`·`rr_registry_status_log`·`rr_audit` + 계보·출처 2표 `rr_panel_calls`·`rr_cluster_alias` + 입력·부분 실패 2표 `rr_requirements`·`rr_snapshot_jobs` 포함) + `_user_credentials`(§8.2.7 열 — `portal_pat_enc BLOB`(Fernet)·`pat_scopes_json`·`pat_jti`·`revoked_at`, 평문 `portal_pat` 열 없음) · `visible_projects(caller)` 단일 함수(§0.6 '투영·MCP 범위') · `.pre-migrate-<ts>` 사본
- [x] `backend/app/routes.py`(prefix `/api`) — `GET /health {ok, app_version, schema_version}` · `GET /me` · `PUT /me/portal-pat`(422 `pat_invalid·pat_email_mismatch·pat_audience·pat_scope_too_broad·pat_expiring·cred_key_absent`, 저장은 `portal_pat_enc` 만) · `GET /meta/taxonomy|adapters` — 실존 `app/api.py`(prefix `/api/v1`, `/meta*` 4종)
  - [ ] 델타: 파일명 `routes.py`·prefix `/api` · `GET /health`·`GET /me`(`portal_pat.scopes`·`revoked_at`·`box.cred_key_present` 포함)·`PUT /me/portal-pat` 추가(`/meta/vocab` 유지)
- [x] `backend/app/mcp_server.py` — FastMCP `hwax-risk`(DNS rebinding 비활성), 도구 6종 시그니처(`risk_get_brief(target_key, brief_token, tier='B')`·`risk_submit_panel_result(…, actor, model?)` 포함) + caller 해석 헬퍼 자리 `mcp_caller(request)`(§8.2.5), 본문 `{error:'not_implemented'}` — 실존 `app/mcp_server.py`(도구 3종 `risk_health·risk_get_taxonomy·risk_get_meta`, DNS rebinding 비활성)
  - [ ] 델타: 3종 제거·6종 시그니처 `not_implemented` · `streamable_http_path='/'`(`tools/list` == 6)
- [x] `backend/app/narrative.py` v0 = `parse_risk_spec` · `taxonomy.py` — 실존 `app/narrative.py`(`parse_risk_spec`+`validate_risk_spec`)·`app/taxonomy.py`
- [ ] `backend/app/adapters/{base,registry}.py` v0(`/tools-map` 도구명 집합 바인딩) · `runner.py` 골격(`panel_loop` 5 s·`sync_loop` 60 s·`nightly_loop` 00:30·`pat_unavailable` 표기) — 실존 없음, 신규
- [x] `backend/app/assets/*.v1.json` 6종(정본) · `backend/app/schemas/{rr_ir, rr_state, rr_diff, risk_spec, seat_opinion}.v1.json` + 유효/무효 픽스처 각 ≥2 — 실존 `app/schemas/*.v1.json` 5종+픽스처, 자산 6종은 `docs/*.v1.json` 에 실존
  - [ ] 델타: 자산 6종을 `backend/app/assets/` 로 이동(package-data 등록)·`docs/` 에서 제거(§0.4.5 문서 디렉터리 금지)
- [ ] `backend/scripts/bootstrap_ra_ontology.py --base <RA REST 오리진>`(6축·12관계, dry-run 기본, 멱등, env `RA_ADMIN_PAT`) · `bootstrap_adh.py`(doc_type 3종·`risk-review-memory`·`external_source=hwax-risk`, env `HWAXRISK_AIDH_API_KEY`) — 실존 없음, 신규
- [x] `backend/tests/{test_boot, test_parity(env HWAX_PORTAL_REPO), test_schemas, test_parser, test_identity}.py` — 실존 `tests/{test_health, test_config_datadir, test_store, test_parse_risk_spec, test_schemas, test_mcp_tools, test_manifest, test_identity}.py`
  - [ ] 델타: `test_boot.py`(3점)·`test_parity.py` 신설 · `test_parse_risk_spec`→`test_parser` · `test_mcp_tools` 6종·`not_implemented` · `test_identity` 되묻기·캐시 1회·위조 헤더 무시 · `test_manifest` 허용 오류 = 루트 `mcp`·`source.ref` 2건
- [ ] `frontend/` — Vite base './' · HashRouter 라우트 5 · 페이지 셸 5 + `SettingsPage` 완성(`GET me`·`PUT me/portal-pat`·box 상태·동의 문구) · `api/risk.api.ts` · `mermaid` 의존 · `pnpm-lock.yaml` — 실존 없음(`app/static/index.html` 플레이스홀더뿐), #28 (ii) 면 P1
- [x] `README.md · checklist.md · context-notes.md` — 실존(+`docs/plan.md` 포인터)
  - [ ] 델타: `hwax_risk.db`→`risk_review.db` · `/api/v1`→`/api` · `/health`→`/api/health` · `schema_migrations`→`_schema_migrations` · 도구 3종→6종 · `.sqlite` 문자열 0건(역사 언급 포함) · context-notes D2 '경로·이름은 이 노트가 정본' → '정본은 포털 plan.md(#28)'
산출물 B — HEAXHub 등록(코드 0)
- [ ] `HEAXHub/integrations/hwax-risk/.portal/manifest.yaml`(매니페스트 전용 디렉터리, 심볼릭 링크 아님) 커밋 → 스캔(즉시 트리거 가능) → `var/sifs/hwax-risk.sif` 빌드 → reconcile 기동 → Caddy `/apps/hwax_risk/*` 자동 라우트 → `var/app_data/hwax_risk/` 생성
- [ ] `var/app_data/hwax_risk/secrets.env`(0600) — `HWAXRISK_PORTAL_PAT`(scopes read) · `HWAXRISK_PORTAL_PAT_RW`(scopes read+write, RA·ADH 쓰기 전용) · `HWAXRISK_HEAX_SERVICE_PAT · HWAXRISK_AIDH_API_KEY · HWAXRISK_CRED_KEY`(Fernet) → `redeploy-app.sh hwax-risk`
- [ ] 게이트웨이 `heax-hwax_risk` 자동 흡수 확인(`gateway_config.json`·재시작 없음)
산출물 C — 포털 창(additive)
- [x] 포털: `backend/config/systems.yaml` 타일 `hwax-risk`(리스크 심사 · jwt-handoff · audience heax-hub · `/heax-hub/api/v1/auth/portal-callback` · auto_post/token · accent rose · category Engineering · sort_order 62) + `POST /systems/reload`
- [x] 포털: `frontend/src/App.tsx` 라우트 `/risk` · `AppHeader.tsx` NavLink '리스크 심사'(카탈로그 조건부) · `pages/risk/RiskLaunchPage.tsx`(launch → 앱 링크 2단, 앱 REST fetch 없음)
- [ ] 포털: `delibTaxonomy.ts`(JobId·JOBS 8행째·JOB_ROUTING·suggestJob append) · `conversations.api.ts` ConvKind · `backend/app/agent/routes.py` `ConvCreate.kind`. 포털 백엔드 라우트·Settings·`.env`·nginx 변경 0
산출물 D — 엔진 additive(A 계획과 동일)
- [x] `deliberation.py`: `_CHAIR_ITEMS['risk-review']`(항목 (2) 지시 무시 문장 · (3)(8) `rejected_in_panel` 문장 포함, §6.6.1) · `_CHAIR_ADVERSARY['risk-review']`(delib-baseline-defender) · doc_title · `_RISK_SEAT_CONTRACT`(+:2044 직후 role 접미 3줄) · `_RISK_READ_TOOLS` · `_RISK_KEEP_TOOLS`(15종 — RA·laminate·열충격 8 + 필드·문헌 7 `get_top_issues`·`query_voc`·`search_voc`·`get_voc_summary`·`get_kg_relations`·`search_scholar`·`search_web`, +`_g` 조립식 조건 ≈3줄 :1897 + `_amap` 상향 + `_narrow` keep or 1줄 :1916) · `_resolve_opts` origin 통과 1줄
- [x] `hwax-deliberate.js`: `CHAIR_ITEMS` · `CHAIR_ADVERSARY` · 제목 삼항 · `RISK_SEAT_CONTRACT`(`_common` 의 «…» 지시 무시 문장 포함) · whenToUse(순수 리터럴)
- [ ] agent-server `/health` 에 `sampling{temperature, top_p, max_tokens, seed?}` 1키 additive(§8.4.1 — 없으면 앱이 `sampling=null` 로 진행)
- [x] `HWAXPortal/scripts/check_chair_parity.py`(`--py ../HWAXAgentServer/deliberation.py --js infra/pipeline/hwax-deliberate.js --contract ../HWAXRisk/backend/app/assets/seat-contract.v1.json`)
- [ ] `delib_metrics.py` 에 risk_spec 파싱 성공률 1종
산출물 E — 문서(포털 `docs/design-risk-review/{plan, checklist, context-notes}.md` · 앱 리포 `HWAXRisk/docs/odb-adapter-contract.md`(정본, 실존 — §2.5.3) · decision-table.md §8.3.7)
통과 기준 (18)
- [ ] (1) `check_chair_parity.py` exit 0 — 결정문·반대석·좌석 계약 16 문자열·제목 바이트 동일, 앱 `test_parity.py` 동일 결과(env 없으면 skip)
- [ ] (2) RA `list_object_types` 에 6축·12관계, `--apply` 재실행 생성 0·기존 15축 17관계 무변경, 앱 코드·`secrets.env` 에 `rat_`·`RA_ADMIN_PAT` 0건
- [ ] (3) AIDataHub doc_type 3종·`risk-review-memory` 존재, 재실행 생성 0, `import?external_source=hwax-risk` dry_run 200(키 없이 401 또는 anonymous 통과 기록)
- [ ] (4) StepForge 직접 읽기 — Caddy `/apps/step_forge/api/projects/{id}/tree` heax 서비스 PAT 로 200 · 무헤더 401 · 게이트웨이 `heax-step_forge` `project_tree` 동일 노드 수 · 앱 코드에 GET 외 메서드 0건 · `gateway_config.json` diff 0(`rest.heax` 불요)
- [ ] (5) 기존 8 chair e2e 결정문 형식 불변 + `_g`/`_narrow` 단위 테스트(apps 유무·chair 유무 조합, `get_agent_session` 항상 부재). `_MATERIAL_TOOLS` 항만 가용성 조건부 — 라이브 `/tools-map` 에 4종이 0건이면 `skip(materialtwin_backend_down)` 이고 대신 material 좌석이 `deferred` 로 편성되는지 확인(§6.5.2·§10 #41)
- [ ] (6) `/심의`+risk-review 단발 3회 — 파싱 ≥2/3 · 반대석 origin=adversary 3/3 · personas origin 보존 3/3 · `extra_seats==∅` 3/3
- [ ] (7) `_persona_round` sysmsg 덤프에서 `[리스크 심사 좌석 계약]`+`[<dom>]` 5/5석 검출, chair=default 에서 0/6
- [ ] (8) apps 지정 상태에서 rel 좌석 `search_objects`(keep) · sim 좌석 `report_part_risk`(app+read) 실호출 SSE ≥1
- [ ] (9) 러너 자격 실측 3항 → context-notes — (a) 서비스 PAT 로 SIF 안에서 `/agent/conversations` conv_id + `/agent/chat` 200·SSE done / (b) 등록 사용자 PAT 로 DynaForge per_user_sso `list_sessions` ≥1, 서비스 PAT 는 0건 / (c) aud 불일치 422 `pat_audience`·`/agent/chat` 401, 만료 임박 422 `pat_expiring`. (a) 실패 시 폴백은 §6.7.1 (B)
- [ ] (10) 앱 pytest 스키마 라운드트립 5종·무효 픽스처 거부 ≥10·`parse_risk_spec` 픽스처 3종
- [ ] (11) 포털 `tsc -b && vite build` + playwright: `/deliberate` Job 카드 8·기존 7 텍스트 불변·`/risk` 버튼 2·`/apps/hwax_risk/api/*` 요청 0·타일 밖 그룹 NavLink 4. 앱 `pnpm build` 통과
- [ ] (12) 포털 무변경 — `openapi.json` diff 0(`/api/risk/*` 부재)·`/agent/conversations` 응답 동일·`config.py`·`.env.example` diff 0·앱 정지 전후 포털/HEAX 응답 코드 동일
- [ ] (13) 앱 기동 3점(로컬·SIF) — `GET /api/health` 200 `{ok, app_version '0.1.0', schema_version 1}` ≤20 s · `POST /mcp` initialize 200 + `mcp-session-id` + `tools/list` 6 · `GET /` index.html. Caddy `/apps/hwax_risk/` 익명 401·인증 200
- [ ] (14) 매니페스트·등록 — `validate_manifest` 루트 `mcp`·`source.ref` 외 오류 0(정확히 2건, `tags` 는 스키마 안) · 스캐너 `by_action` 1건 · 카탈로그 `hwax_risk beta company` · `sif_build_hwax-risk.log` 성공 · `var/sifs/hwax-risk.sif`+`.hash` · state 파일 `hwax_risk.json` · Caddy 라우트 2건
- [ ] (15) 게이트웨이 자동 흡수 — heax `/api/v1/mcp/servers` 에 `hwax_risk` 1건 · 재시작 없이 `/tools-map` 에 `heax-hwax_risk` `risk_*` 6종(len +6, 타 백엔드 불변) · `gateway_config.json` diff 0 · `risk_get_registry` → `not_implemented`
- [ ] (16) 데이터 경로·app-data 왕복 — `var/app_data/hwax_risk/{risk_review.db(user_version 1, 41+2표), origin.json(hostname dev), exports/}` · `appdata-to-drive.sh` → Drive `latest/` tar 에 `.backup` 스냅샷 · 비운 뒤 `appdata-merge-from-drive.sh` 복원 `integrity_check ok` · `redeploy-app.sh hwax-risk` 후 보존
- [ ] (17) 신원 해석 — `GET /api/me` 쿠키 `source:'cookie'` / Bearer `source:'bearer'` / 위조 `X-Heax-User-Email` 무시 / 무토큰 Caddy 401 / 10회 호출에 `/auth/me` 1회. `PUT /api/me/portal-pat` 오류 6종 422 · 정상 등록 `_user_credentials` 1행 · null 삭제 0행
- [ ] (18) 자격 최소 권한·암호 보관(§5.1 원칙 10·§8.2.7) — (a) `scopes:['read','write']` PAT 는 422 `pat_scope_too_broad`, `['read']` 는 200·`GET /me.portal_pat.scopes==['read']` / (b) DB 덤프에 PAT 원문 0건(`portal_pat` 열 부재)·키 없으면 422 `cred_key_absent`+`box.cred_key_present=false`·로그·`/me`·export 에 원문 0건 / (c) 포털에서 폐기 후 60 s 안에 `revoked_at` 표기·다음 잡 `credential='service'`·폐기 목록 형식 위반 시 직전 목록 유지+경고 1줄 / (d) 서비스 2키 발급·`test_no_write_tools.py`(runner 에서 `PORTAL_PAT_RW` 참조 0·좌석 화이트리스트에 쓰기 도구 0)
얻는 것 — 핸드오프 카드·MCP `chairTemplate:'risk-review'` 로 evidence 기반 단발 심사(결정문 8항목+risk_spec+기준선 옹호 지정석) + 포털 메뉴 → HEAX SSO → 앱 셸(`/apps/hwax_risk/`, PAT 등록) + 게이트웨이 `risk_*` 6종 노출 + dev app-data 백업 경로 검증

## P1 단일 과제 IR 스냅샷·상태 평가·게이트·규칙(MCAD) (§9.2) — 전부 앱 리포
- [ ] `backend/app/risk_store.py` P1 표 쓰기 경로(§9.2 산출물 목록 — `rr_curation_queue(kind='suspect_text')` 포함) · `adapters/{mcad,ecad_stub}.py`(Caddy `/apps/step_forge/api` GET, heax 서비스 PAT + 게이트웨이 `heax-step_forge`) · `ir_builder.py` · `sameas.py` v0 · `state.py` · `render.py`(판단어 린터) · `routes.py`(projects/sources/snapshots/dims/requirements/refs/rule_hits) · rules 시드 7종(R-007 포함) · `frontend/src/pages/{RiskHomePage,ProjectPage,SnapshotPage}.tsx`(HashRouter) · '단발 심사 열기'(포털 `/deliberate` 새 탭 + E0·E1 클립보드) · `prior_evidence` v0(E0·E1·E9) · `ra_client` v0(게이트웨이 MCP) · `export.py` v0(`GET /api/export`·`POST /api/import`) · 골든 `backend/tests/golden/sif-e2e.ir.json`
- [ ] 표기층 위생·인젝션(§3.4.1) — `render.sanitize_source_text(s, kind)`(NFC·제어문자/zero-width/양방향 제어 제거·개행 압축·종류별 상한·`«…»`, `ir_hash` 불변) · `INJECTION_LEXICON`(inj-1.0, X01~X10) 적중 시 `«[suspect_text …]»` 자리표시자 + `rr_ir.warnings` + `rr_curation_queue(kind='suspect_text')` · `rr_findings.recall_eligible`·`rr_character.recall_eligible` · `GET /curation`·`PUT /curation/{id}`(`approve_text`·`approve_recall`·reject) · `CurationQueue.tsx`(`#/curation`) · SnapshotPage warnings 붉은 칩 · 브리프 프레이밍 줄에 «…» 문구(≤80자)
- [ ] 소유·수명주기·등급·감사(§5.2.1·§5.2.5 (3a)·§5.2.6) — `rr_project_members`(과제 생성 시 owner 행 자동)·`rr_gate_acks`·`rr_audit` 쓰기 경로 · `require_role()`·`corpus_projects()`·`audit()` 3함수 · `PATCH /projects/{id}`(lifecycle·corpus_excluded·classification) · `GET/PUT members` · `POST transfer` · `POST purge`(회수 러너는 P3) · `GET projects/{id}/audit` · `POST|DELETE /snapshots/{id}/gates/{G}/ack` · `POST /projects` classification 필수·`duplicate_of` 응답 · `GET /export` 자격·`X-Risk-Classification-Max`·`risk_export_retain_days` · `appdata-to-drive.sh` 래퍼(`--exclude`+age, 키 없으면 health `backup_unencrypted`) · 화면(RiskHomePage 등급·lifecycle·역할 칩·필터, ProjectPage 멤버/이력 탭·이양·폐기, GateBanner ack 버튼)
- [ ] 투영·MCP 경계 기본값(§5.1 원칙 9·§5.2.2 A) — `rr_projects.mcp_visibility`(기본 `private`)·`mcp_visibility_by/at` · `PATCH /projects/{id} {mcp_visibility}` owner 전용(editor 403 `role_insufficient`)+`rr_audit(project.mcp_visibility)` · `ProjectPage` '조직 공개' 토글과 보류 op 수 표시 · `risk_store.visible_projects(caller)` 를 REST 조회 경로에 먼저 적용
- [ ] 요구·제품 입력(§2.8b·§5.2.2 A) — `rr_requirements`(kind `dim_limit`·`scenario`·`standard`, status `candidate|confirmed|waived`, `inherited_from`) 쓰기 경로 · `GET|POST /projects/{id}/requirements`·`PUT /requirements/{id}`(waive 사유 필수)·`POST /projects/{id}/requirements/inherit` · `rr_projects.product_code`·`product_refs_json`·`predecessor_product_code` + 등록 폼 제품 다중 선택·'제품 연결 없음' 배지 · `state.py` 대조(`sig:req.margin`·`sig:req.scenario_coverage`·`missing.req_absent`·`scenario_uncovered`, 요구 편집은 ir_hash 불변·rr_states 재계산) · 규칙 R-007 `req_margin_negative`(시드 7종) · `char:constraint` 축 2값 + `character-vocab.v1.json` 8축 · `rr_findings.requirement_ref` · ProjectPage '요구' 탭 + SnapshotPage 여유 표(margin 오름차순) · E0 요구 1줄·E3 limit/margin 열
- [ ] 소스 버전·응답 계약(§2.5.1·§2.13.1·§3.3.6) — probe 의 `*_system_status` 1회 호출 → `sources[].app_version`·`rr_snapshots.app_versions_json`(ir_hash 제외) · `IrAdapter.response_contract` 도구별 필수 JSON pointer 검사 → `warnings.source_schema_drift`·`degraded.schema_drift`·`rr_snapshot_calls.contract_ok`·`contract_missing_json` · `risk_source_drift_block` 스위치 · `comparability` 5키(`app_version_parity`·`adapter_parity`·`source_schema_parity`·`capture_parity`·`primary_source_parity`) · `§7.7 source_app_versions` 스탬프 + ProjectPage/ComparePage '파서 세대 다름' 배지 · 드리프트 픽스처 2종
- [ ] 스냅샷 잡·부분 캡처·모델 상한(§2.11.3·§5.2.2 B) — `rr_snapshot_jobs`(`queued|running|done|partial|failed`, `error_json{stage,kind,tool,call_id}`, 재기동 시 running→failed) · `rr_snapshot_calls.job_id` NOT NULL·`snapshot_id` NULL 허용(실패 원문 30일)·`reused_from_call_id` 재사용 · 필수/선택 호출 실패 분기(`<kind>_capture_failed` vs `capture_partial`·`iface_kinds_absent`) · G3·R-001 `pass=null` · diff `excluded_reason='capture_partial'` · `risk_max_leaf`(1500)·`risk_max_interfaces`(6000) → `409 model_too_large`·`allow_large`(예산 600 s·`degraded.large_model`) · `list_interfaces` 절단 정렬 고정 + `stats.interfaces_truncated` · 헝가리안 `800×800` 초과 시 `fuzzy_skipped_large` · 부분 캡처 픽스처 2종
- [ ] MCAD 부재 경로(§2.2·§2.11.3·§2.12) — `primary_source`(mcad>dyna>ecad, ir_hash 입력 아님) · '요청 kinds 전부 도달 불가일 때만 409' · `missing.mcad_absent`·`degraded.mcad_absent` · `asm_key` 를 `_group_of` 한 층/ecad side 로 · 예약 치수 primary_source 기준 · G3·G4 `pass=null`·G6 단위만 · `char:analysis:{mcad_present,sim_only,ecad_only}` + facet `unknown` 자동 진술 · `risk_mcad_domains` deferred(§6.4.1) · 유효 픽스처 2종(dyna 단독·dyna+dyna_result)
- [ ] 정찰 실측 계약(2026-08-31, §2.5.1·§2.13) — 호출 예산 **정상 REST 5 + MCP 3 / `mcp_degraded` MCP 7**(§2.13.3, REST 5 는 `GET /projects/{id}`(tol_config) 포함이고 `/interfaces?limit=5000` 가 `list_interfaces`×4 를 대체) · `adapters/registry.py` **suffix 이름 매칭**(`name == want or name.endswith('_'+want)`, 게이트웨이 충돌 접두 `backend_key.replace('-','')+'_'`, 다의면 `ambiguous_tool_name`) · `adapters/base.normalize` **타입 정규화 5행**(`cross_file` int↔bool · `orphans` 배열↔int · `has_geometry` int · `counts` 키 생략↔4키 · `unmatched_rules` int↔배열, 정의 밖 값은 `warnings.type_unexpected`) + `rr_ir.v1.json` 무효 픽스처 7종(`cross_file` int 1건 추가) · **절단 감지식** `truncated = sum(interface_graph.counts.values()) > len(interfaces)`·상한 MCP 500/kind · REST 5000 · **브리지 조인 키**(`mesh_key` 폐기 → `join_key = source_path` 또는 `step_file+'/'+source_name`, 중복 시 `ambiguous_bridge_key`) · **계면 끝점은 REST 필수**(`n{seq}` 를 이름·path 로 못 잇는다 → `mcp_degraded` 에서는 계면 엣지 0건·`iface_kinds_absent`) · `provenance.iface_row_id_at_capture`(REST 판 행 id) · `interface_graph` 는 limit 클램프가 없어 MCP-only 여도 엣지 전량 위상 확보 · kind 강도 순위는 자체 파생 상수(소스에 rank 필드 없음)
- [ ] degraded 3종·G6 `unknown_blocking`(§2.2·§2.12·§3.2.2) — `no_depth_seq`·`no_auto_named_flag`·`tree_truncated` 를 `degraded` 어휘에 추가하고 `mcp_degraded` 가 앞 둘을 항상 동반 · `project_tree` 노드 > 500 이면 `nodes` 키 소실 → `tree_truncated`+`mcad_capture_failed` · 게이트 레코드에 `reason` 열 추가(`mcad_absent`·`capture_partial`·`unit_only`·`warnings_unavailable`·`unit_unknown`) · G4 는 `mcp_degraded` 에서 `pass=null(warnings_unavailable)` · **G6 는 `pass=null` 이어도 `blocking=true`**(`blocked = pass is False or (pass is None and reason=='unit_unknown')`, ack 불가, `POST /diffs` 409 `gate_blocked{reason}`) · 게이트 픽스처 7 → 8(`gate_f8_unit_unknown`) · `GateBanner` 는 ack 버튼 대신 'REST 원문 재캡처' 안내
- [ ] `context.corpus_usage` 봉투 승격(§2.2·§2.5.2·§2.11.3) — `sources[dyna].context` 폐기, 봉투 최상위 `context.corpus_usage{app_key, materials, sections, contacts, sessions, files, jobs, fetched_at}` · 캡처 3단계를 **3a 전사 집계(자격 무관, `corpus_summary`·`material_usage`·`section_contact_usage`·`operation_usage`) / 3b 세션·파일(자격 (b) 필요)** 로 분할 · `dyna_absent` 여도 3a 수행 · ir_hash 제외 · 4호출 전부 실패면 `null`(`degraded` 불변)
- [ ] rule_hits `evaluable`·`not_evaluable_reason`(§3.2.6) — 항목마다 두 필드, `evaluable=false` → `pass=null`+사유 `source_absent|degraded|truncated` · R-004·R-005 가 `mcp_degraded` 에서 `pass=true` 가 아니라 `degraded` · 등록부·delta 선례·패턴 분모에서 제외 · SnapshotPage·E9 에 `평가 불가(<사유>)` 표기
- [ ] mcad 격리 부재(§0.1.6·§2.13.3·§8.2.4) — 러너 자격 (a)/(b) 시야 가설을 **dyna 전용**으로 좁힘(StepForge 는 owner 컬럼·`per_user_sso` 없음 → 호출자 전원 같은 `/data`) · `POST /projects/{id}/sources` 응답 `warnings ∋ stepforge_no_isolation` + ProjectPage mcad 카드 상시 경고 · `classification` 필수는 기존 규정 유지 · (a) 자격의 dyna 호출은 401 이 아니라 **200·0건**이므로 `quality_json.empty_result_n` 과 `low_tool_use` 로 잡음(§6.5.5)
- [ ] D6 모델 출처 — `rr_panels.model_json`·`rr_coverage.model`(v1 DDL, §5.2.2 E) · `runner.snapshot_model(origin)`(agent-server `GET /health` 픽스처 → `model_json{…, captured:'health_snapshot'}`, 시작·종료 불일치 → `quality.flag=model_changed_midrun`, 불통 → `unavailable`/`'unknown'`) 단위 테스트 · export JSONL 에 두 컬럼 포함(§9.2 (13))
- [ ] 통과 23항 — sif-e2e 노드3·엣지2 일치·ir_hash 결정론 / Caddy GET 원문만·StepForge sqlite mtime 불변 **+ 호출 예산 REST 5·MCP 3(폴백 MCP 7) 초과 0** / world_center ±0.01mm(REST 가용 전제) / G1~G7 픽스처 **8**(+`mcad_absent`·`capture_partial`·`unit_only`·`unit_unknown` 의 `pass=null` 사유, `unknown_blocking` 은 diff·타깃 409·ack 422) / 500파트 누락0·≤10s / 린터 판단어 0 / rule_hits 7종·payload_hash 동일 **+ `evaluable`·`not_evaluable_reason`**(`mcp_degraded` 에서 R-004·R-005 는 `pass=null`) / rr_snapshot_calls gzip / ckey 결정론 / prior_evidence 라인 ≤CAP·드롭 0 / 앱 ruff·pytest·pnpm build·포털 `/deliberate` 스모크 / export→import 라운드트립 행 수 동일·409 `schema_mismatch`·재import inserted 0 / 모델 출처 컬럼 2·`snapshot_model()` 픽스처·`model_changed_midrun`(§9.2 (13)) / **두 계정 시나리오**(비멤버 403·editor 200·viewer 쓰기 403·이양 후 하위 11표 owner_sub 전부 갱신·비-owner purge 403, (14)) / **수명주기·코퍼스 필터**(사유 없는 제외 422·제외 시 delta_priors 재합산·sif-e2e 는 `fixture` 로 등록·archived 는 코퍼스 유지, (15)) / **등급·반출**(classification 없으면 422·`X-Risk-Classification-Max`·그룹 밖 403·tar 에 평문 DB 0건·age 사본 1건·키 없으면 `backup_unencrypted`, (16)) / **게이트 ack**(pass 불변·G6 422·빈 사유 422·`gates_hash_stale` 409·E0 에 ack 사유, (17)) / **감사 로그**(자동 전이 0행·`UPDATE|DELETE rr_audit` 문자열 0건·export 에 3표 포함, (18)) / **표기층 위생·인젝션**(픽스처 10종 — `ir_hash` 불변·원본 바이트 불변·발췌 전부 «…» 안·적중 7종 자리표시자+큐 7행·프레이밍 줄 ≤80자·`approve_text` 복원·린터 0, (19)) / **요구 입력과 여유**(margin 손계산 일치·치수 null 은 `known=false`(0 아님)·요구 편집에 ir_hash 바이트 불변·요구 0건이면 R-007 `pass=null`+E0 `요구 미등록`·400 `unit_mismatch`·waive 사유 없으면 422·inherit 2회 중복 0·`req:` dangling 강등·`margin ≤0` 인데 `judgement='OK'` 면 `undetermined` 보정, (20)) / **소스 버전·응답 드리프트**(`app_versions_json` 채움·도구 막으면 `app_version_unknown`·드리프트 픽스처 2종에서 null 흡수 0·`contract_ok=0`·`contract_missing_json`·`risk_source_drift_block` 분기·버전만 바꾼 두 캡처의 ir_hash 동일, (21)) / **부분 캡처·대형 모델**(선택 실패 → `partial`+`capture_partial`+G3/R-001 `pass=null`, 필수 실패 → `<kind>_capture_failed`, 전 kind 실패 → `failed`+`snapshot_id IS NULL` 원문 `GET /refs` 200, 재시도 `reused_from_call_id`, 리프 1501 → 409 `model_too_large`, `allow_large` → `budget_s=600`+`large_model`, 강제 종료 후 running→failed, (22)) / **어댑터 실측 계약**(suffix 이름 매칭으로 접두형 `job_status` 발견·다의면 `ambiguous_tool_name` / cross_file·has_geometry·counts·orphans 정규화와 `type_unexpected` / `interface_graph.counts` 기준 절단 감지와 REST 5000 에서 `truncated=false` / `mesh_key` 없이 브리지 성립·`join_key` 접두·중복 시 `ambiguous_bridge_key`·앱 코드에 `mesh_key` 문자열 0건 / `dyna_absent` 여도 `context.corpus_usage` 채워짐·`sources[*].context` 부재·ir_hash 불변, (23))
- [ ] **선행 조건 B1~B6**(§9.2 표 — 닫히기 전에는 해당 통과 기준을 합성 픽스처로만 채운다) — B1 heax 서비스 PAT 발급(§10 #2·P0 (4), 우회 없음·최대 단일 레버 — 미발급이면 REST 채널 0회·계면 엣지 0건·G6 `unknown_blocking` 으로 diff·타깃 불가) / B2 골든 `sif-e2e` 재파싱·재검출(§10 #15, 미실행이면 첫 캡처가 `volume_null_pre_d168` 확정) / B3 계획 내부 모순 3건(호출 예산·G6 분기·`tree_truncated`) — **이 개정으로 닫힘** / B4 실무 규모 STEP 1건 업로드(§10 #15 병기) / B5 DynaForge 세션·K파일·리포트 각 1건(§10 #4·#15, P2 (8)(9) 선행) / B6 `heax-materialtwin_web` 기동(P0 (5) material 항 skip·§10 #41)
- [ ] 선행(§10 15·2·36·37·38·39·41): 기존 StepForge 프로젝트 재파싱·재검출은 사용자 실행, 골든 프로젝트 준비, heax 서비스 PAT 발급, 요구 입력 주체·규격 원천 지정, 드리프트 차단 강도, 제품 대응표 유지 주체, 모델 상한 값, material 좌석 처리
얻는 것 — 앱 화면의 과제 등록(제품 연결)·요구 규격과 여유 표·스냅샷 현황판·게이트·성격 씨앗·규칙 히트·소스 앱 버전과 부분 캡처 표기·단발 심사 브리프 + export/import v0

## P2 Dyna 어댑터·same-as·원장·3층 diff·summary_text (§9.3) — 전부 앱 리포
- [ ] 키 계보(§2.7.1) — `name_norm_canon` 1단계를 자기 계보 코드로 한정 · `POST /vocab/synonyms`·`POST /vocab/stop-tokens`(마이너/메이저 승급, 409 `recompute_pending`) · `rr_part_keys.vocab_version` · `backend/scripts/recompute_part_keys.py`(멱등·dry-run 기본, 기존 ckey·ir_hash 불변) · ProjectPage '사전' 탭 + 재계산 필요 배너 · `GET /health.warnings[] vocab_recompute_pending`
- [ ] `adapters/dyna.py`(게이트웨이 `heax-kooremapper_mcp`, 러너 자격 (b) 사용자 PAT / (a) 서비스 시야 = `dyna_absent`) · `sameas.py`(사다리 7단·헝가리안·원장 재적용·ckey 자동 승계) · `diff.py`(3층·임계·comparability·의미 이벤트) · `rr_diffs/rr_diff_events` · `mcp_server.py` `risk_get_snapshot`·`risk_get_diff` 실구현 · `components/{SameAsResolver,GateBanner,DiffView}.tsx`·`pages/ComparePage.tsx` · `prior_evidence` v1(E2~E4) · 합성 픽스처 6종 · `ra_client` design_diff · `export.py` 표 추가
- [ ] MCAD 부재 쌍·비교 가능성(§2.6.2·§3.3.6) — dyna↔dyna 쌍은 `exact_path` 건너뜀(4·5단계가 대응) · 유효 픽스처 2종(dyna 단독·dyna+dyna_result) · `comparability` 5키 구현과 `caveat='parser_differs'`(제외 아님)·`excluded_reason` 3종(`source_drift`·`capture_partial`·`truncated`) · `app_version_parity=false` 면 `char:change_style:dimension_tuning` 미부여
- [ ] 통과 14항 — 합성 6종 이벤트 정확·self-diff 0 / 브리지 same-as 100% / 교란 30쌍 정밀 ≥0.95·재현 ≥0.9 / 원장 manual_ledger 복원 / tol·result parity 제외 / summary ≤2000·린터 0 / 500·2000 diff <5s + RSS 피크 기록(§10 27) / (a) 서비스 PAT 는 `list_sessions` 0건 → `dyna_absent`·예외 0·`kr_` 문자열 0건 / energy_flow src/dst 확정 / G2 fail 시 semantic.blocked_by / ckey·subject_key 일치 ≥0.95 + §4.9 픽스처 승계 / **사전 편집과 재키**(남의 과제 등록에도 기존 ckey 불변·마이너/메이저 승급 규칙·409 `recompute_pending`·재계산 후 ir_hash 불변·별칭 행만 증가·2회 실행 멱등, (12)) / **mcad 없는 과제 심사**(`kinds=['dyna','dyna_result']` 만으로 202·`primary_source='dyna'`·`mcad_absent`·G3/G4 `pass=null`·G6 dyna 단위만 pass·`asm_key` 한 층·예약 치수 dyna stats·mcad 유래 signals `known=false`·`char:analysis:sim_only`+`unknown` 자동 진술·K파일 계보 두 스냅샷 diff 200 과 `result_delta` 생성·전 kind 도달 불가일 때만 409, (13)) / **비교 가능성 5키**(`app_version_parity=false` 는 제외가 아니라 `caveat='parser_differs'`+confidence 강등·`source_schema_parity=false` 는 `source_drift` 제외·`capture_parity=false` 는 `capture_partial` 제외+의미 이벤트 0·`primary_source_parity=false` 는 rollup delta 0·`source_app_versions` 스탬프 2값, (14))
- [ ] 선행: P0(9)(b) 결과, DynaForge 세션·K파일·리포트 각 1건(§9.2 선행 조건 B5 · §10 15·15a — 미충족이면 §2.5.2 표는 '소스 확정·실호출 미검증' 상태이고 (8)(9) 는 합성 픽스처로만 채운다), §10 4·35
얻는 것 — 앱 화면의 두 과제 비교(3층 diff·결론 없는 요약)·pair 단발 심사(E0~E4)·MCAD 없이 K파일만 있는 과제의 심사 경로·게이트웨이 `risk_get_snapshot/diff`

## P3 risk-review 패널 e2e·서술 저장 (§9.4) — 전부 앱 리포
- [ ] `narrative.py` 완성(cites·dangling·evidence_grade·seat_opinion·E0c·E0~E4+E9) · `runner.py` 단일 패널(§6.7.1 (A) 포털 `/agent/chat` 포털 PAT Bearer · 자격 (a)/(b) 결정 · `/agent/conversations kind:'risk-review'` · SSE 캡처 · 사전 예산 · 커버리지 · `quality_json.{call_path,credential,call_groups}` · (B) 폴백 규칙) · `registry.py` · `character.py` · `ra_client`(게이트웨이 MCP, 항상 서비스 PAT)/`adh_client`(`external_source=hwax-risk`) · 표 7종 · `routes.py`(targets/jobs/panels/complete/resync/refs) · `mcp_server.py` `risk_get_registry·risk_claims_for_ref·risk_submit_panel_result` 실구현 · `TargetPage` 최소 + `PanelTranscript`(앱 DB 렌더) · SSE 픽스처 3종
- [ ] 패널 실행 원문 보존(§5.2.2 F·§6.7.2 7단계·§5.6.4) — `rr_panel_calls`(SSE·`events[].text`·`tool_inject`, `call_id '<panel_id[:8]>-<seq:03d>'`, `result_gz` 전문·`sha256`, `conv_id`/`activity_idx`) · `rr_panels.brief_gz`·`brief_hash`·`brief_item_hashes_json` + `GET /panels/{id}/brief` · `quality_json.brief_drift`·`neg_precedent_cited_n` · `rr_roster.role_sha`·`persona_rev`·`rr_seat_opinions.persona_rev` · `model_json.sampling` · `tool:panel:` 참조와 `GET /refs` 앱 우선·`payload{raw, canonical}` · §5.5.1 외래키 표 정정 · `PanelTranscript` 탭 3개
- [ ] 기각·반증 보존(§4.3.1·§4.7.1·§6.6.1) — risk_spec `findings[].status` `open|rejected_in_panel`(그 밖은 `open` 보정+`parse_warnings`) · `rr_findings`·`rr_registry` CHECK v1 에 `rejected_in_panel` · `rr_registry.rejected`·`family_key` · 병합에서 support 미가산·전원 기각이면 행 status 전이 · `PUT /registry/{cluster}/status {status:'open'}` 되돌리기(evidence 필수) · verdict 후보 제외
- [ ] 회수 격리(§3.4.1·§6.11) — MCP `actor_verified=false` 패널 산출과 `suspect_text` 원자를 `recall_eligible=0` 으로 · `PUT /curation/{id} {decision:'approve_recall'}` + `rr_audit(recall.approve)`
- [ ] 키 별칭(§4.3.2·§5.9.5) — `rr_cluster_alias`(reason 5종·revoke)·`resolve_cluster_key()` 체인 ≤5 · `rr_iface_alias.status`·`rr_sameas.prev_status` · rekey 6단계 트랜잭션(원장 이력 → 영향 범위 → 별칭 append → `registry.merge` 재실행 → priors/patterns 재계산 → `rr_audit(action='rekey')`)
- [ ] 등록부 status 주체·이력·사람 finding(§4.7.1·§7.6·§5.2.6) — `rr_registry` status 5열(`status_source`·`status_decided_by`·`status_decided_at`·`status_note`·`status_basis_json`)·`needs_review_json`·`human_n` · append-only `rr_registry_status_log`(`applied=0` 시도 포함) · 훅 우선순위(사람 행에 자동 라벨 → status 불변·큐 `conflict_with_human`·priors 미계수) · 재제기 escalated(sev3↑ OR grade↑ OR support+2) · `PUT /registry/{cluster}/status` evidence/note 필수·`status_log_seq` · `POST /targets/{key}/findings`·`PUT|DELETE /findings/{id}`(origin·author_sub·`#H<n>`·LLM 행 불변) · `POST /projects/{id}/purge` 회수 러너 6층(§5.2.6) · `GET /targets/{key}/audit`
- [ ] 투영 범위 = 정본 범위(§5.1 원칙 9·§5.3.1·§5.4.2·§5.5.3·§8.2.5) — RA 네 record 축에 `owner`·`visibility` 속성 추가(부트스트랩) · `ra_client` 가 `mcp_visibility='private'` 과제의 네 축 객체를 만들지 않고 `external_sync.ra='withheld'`+`pending_ops` 보존 · ADH 태그 `hwax:owner:`·`hwax:vis:` 부착과 `reason='vis_retag'` 재부착 op · `adh_client` 검색 호출에 `required_tags` 강제(인자 없는 호출 0건) · MCP 읽기 4종 caller 판정(`mcp_caller` → `visible_projects` → `{error:'not_visible'}`)과 `risk_get_brief` 의 `brief_token` 대조(`rr_panels.brief_token_hash`·`brief_token_exp`, `GET /targets/{key}/brief` 가 발급) · `PATCH {mcp_visibility:'org'}` 의 `resynced` 응답
- [ ] 통과 23항 — 파싱 성공·findings ≥3·gains ≥1·facet 8·dangling 0 / 도구 사용률 ≥80%·IR 인용 ≥50%·contested ≥1 / personas 집합==seats∪adversary·extra_seats==∅·human_note 부재 / SSE 귀속 ≥95%(events[] 경로 포함) / conv_store kind='risk-review'·owner=PAT sub·`/deliberate` 0건·`call_path='portal'`·`credential` 일치 / UPSERT 중복 0·agents 실전문가 0·`rat_` 0건 / 70분 뒤 (b) 등록 PAT 그대로 성공(재발급 없음)·만료 임박 픽스처 (a) 강등 / 사전 예산 ≤120·≤20분·over_budget 재실행 0 / MCP `risk_submit_panel_result`·REST complete 동일 파서·evidence_only·`actor_verified:false`·owner_sub 승계 / header_mismatch 픽스처 / 회귀 0·openapi diff 0 / 병합 멱등 / 폴백 (B) — 기본 꺼짐·dev 켜면 3회 후 `agent_direct` 완주·401 은 `pat_unavailable` / `resync` → pending·비소유자 403 / **status 주체·이력**(evidence 없이 verified 422·log seq 1행·자동 라벨은 `applied=0`+큐·priors 미계수·재병합에 5열 보존, (15)) / **재제기 escalated**(sev3↑ 재제기 시 플래그·status 불변·verdict 후보 포함·동일 근거 재제기는 플래그 0, (16)) / **사람 finding**(cites 0 → 422·`support` 불변·`human_n=1`·`n_raised_human=1`·LLM 행 수정 422·expert 지표 분모 제외·`key='human'` 분리, (17)) / **폐기 회수**(원문 NULL·해시 보존·priors 에서 제외·`remaining[]` 명시·타 과제 `reg:` 인용 status 불변·코드 오입력 422, (18)) / **패널 실행 원문**(호출 6건 → `rr_panel_calls` 6행·sha256 일치·conv_store 삭제 뒤에도 `GET /refs/tool:panel:` 200·레거시 `tool:conv:` 해석·export→import 후 dangling 0·`brief_gz` 라운드트립·`brief_drift` 정확·`sampling` 없으면 null, (19)) / **패널 기각 보존**(`rejected_in_panel` 저장·support 미가산·`rejected=1`·전원 기각 시 행 status·verdict 제외·재제기 시 open 복귀·`open` 되돌리기 evidence 422, (20)) / **회수 격리**(MCP 산출 `recall_eligible=0`·웹 산출 1·나머지 열 바이트 동일·`approve_recall` 뒤 1·등록부에는 그대로, (21)) / **키 별칭 재키**(별칭 확정 → 두 행 병합·support=distinct 패널 수, revoke → 재분리·priors 재합산·옛 `reg:` dangling 0·6홉 409, (22)) / **투영 범위**(private 과제 패널 1건 후 RA 네 축 0건·`project` 헤더만·`withheld`+보류 op, ADH 태그 `hwax:vis:private`·타 범위 검색 0건, MCP 읽기 4종 넷 다 `not_visible`·`mcp_not_visible` +4, `brief_token` 없음/틀림/만료 3경우 `brief_token_invalid`·정상 토큰 200, `mcp_visibility:'org'` 뒤 op 방출·4호출 200·태그 재부착, editor PATCH 403, (23))
얻는 것 — 등록부·성격 서술·좌석 의견이 3층(앱 DB 정본·RA·AIDataHub)에 저장되는 완결 심사 1건 + MCP 결과 원장 반영 + 러너 경로 (A) 실증

## P4 커버리지 원장·편성·배치 러너·완결 판정·통합 보고서 (§9.5) — 전부 앱 리포
- [ ] `planner.py`(Tier A/B/C·라운드로빈·인접·deferred·carried·결정론) · `rr_roster/rr_coverage`(부분 유니크 `rr_cov_active`)·`rr_jobs` · `runner.py` 배치 모드(경로 (A) 동일) · C1~C3 · 통합 보고서 v1/v2/v3(게이트웨이 MCP) · `TargetPage` 완성 + `CoverageHeatmap/PanelRunner`(히트맵·자격 표시·미착석 배지·verdict 확정) · `GET /api/meta/metrics` · `delib_metrics.py` 지표 6종(원천 앱 REST) · `tests/test_coverage_sm.py`
- [ ] 사람 개입 원장(§6.8.2·§4.7.3) — `PUT /targets/{key}/coverage/{agent_key}`(skipped·carried 되돌리기, 사유 필수·`status_source='human'`·`decided_by`) · `rr_jobs.state_by`·`state_at`·`credential_email`(§0.1.6 (b) 후보 순서) · `POST jobs` 의 `exclude_evidence` → `params_json`·`rr_panels.evidence_excluded_json`·`rr_audit(brief.exclude)` · 통합 보고서 minutes '사람 개입 요약'
- [ ] 통과 14항 — C1 도달(RA 백엔드 불통/403 에서도, 포털 PAT 유효) / 원장 불변식 / 편성 결정론·동시 편성 이중 착석 0 / 실패 주입 재시도→skipped / ecad deferred ≈105석 / carried 조건·되돌리기 / Tier B ≤6h·슬롯 ≤2·일일 24 / done_weak→C2 strong / 미착석 배지 N 일치 / close_level C2/C3 동작 / 수확 체감 정지 / `redeploy-app.sh hwax-risk` 재기동 복구·conv_id 연결 유지 / **사람 개입 주체**(skipped 사유 없으면 422·`decided_by` 기록·자동 정지는 `state_by='code:*'`·minutes 요약이 `rr_audit` 건수와 일치, (13)) / **동료 잡의 자격 기록**(`credential_email` 이 (b) 후보 순서 첫 적중과 일치·둘 다 미등록이면 `service`, (14))
- [ ] 결정(§10 5·6·7·6a·6b·12a): 로스터 도메인 15·ECAD 6·xd 전원 / 기본 마감 C2 vs C3 / carried 90일 / 패널 LLM 상한 120 / 야간 창·concurrency / DOCX 불요
얻는 것 — "전체 HW/XD 전문가 한 번씩" 회계(C3 문자적 충족, C2 비용 타협)와 레벨별 리스크 심사 보고서

## P5 재사용 루프 (§9.6)
- [ ] 앱: `prior_evidence` 완성(E5~E8·별칭·kNN·hybrid·agent_search 예산표) · `rr_delta_priors/rr_iface_alias` · `rr_part_keys` merge/rename UI · `mcp_server.py` `risk_get_brief(target_key, tier='B')` 실구현(게이트웨이 흡수는 P0 완료, 설정 변경 0) · 성격 승격 UI · `RecallPreview` · `/api/precedents` · `/api/projects/{id}/similar`
- [ ] 엔진·포털: `deliberation.py` `_RISK_READ_TOOLS['heax-hwax_risk']`(registry 발견값 앱키) · `HWAXPortal/infra/pipeline/hwax-risk-review.js`(args `{targetKey, tier, panels?, actor?}`, 포털 REST fetch 없음) + `sync-workflows.sh`
- [ ] 필드·VOC·문헌 근거(§5.6.1 E10·§6.5.2·§7.6) — `field_evidence(target)`(product_refs_json → product_code → predecessor_product_code, 없으면 결측 문구) · `get_top_issues`·`query_voc`·`search_scholar` 호출 원문을 `rr_panel_calls(source_kind='brief')` 에 저장하고 24 h 재사용 · `voc:`·`paper:` 참조와 등급 매핑(§0.2.1 (5)) · `evidence_profile.field` · `_RISK_KEEP_TOOLS` 필드·문헌 7종 실호출 SSE · `taxonomy.voc_map` 시드 12행 · 라벨 경로 1·2 의 (a) 제품 교집합 규칙·경로 4 product_code 조건 · 지표 `field_evidence_rate`·`req_consistency`·`req_coverage`
- [ ] 부정 선례·상태 필터(§5.6.1·§5.6.3·§7.6) — E5 세 블록 `[E5+ 살아 있는 선례]`(700자)·`[E5− 기각·반증 선례]`(300자·`risk_neg_precedent_lines` 6줄)·`[E10 필드·VOC·문헌 근거]`(500자·`risk_field_evidence_lines` 5줄), 합 1500자이고 E1 −250·E6 −150·E9 −100 재배분으로 항목 수 12·합 10600 불변 · E7 상태 필터(`dismissed`·`rejected_in_panel`·`superseded` 제외)와 `[이전 정의]` 접두 · AIDataHub `status:*` 태그 부착·status 전이 시 `reason='status_retag'` 재부착 op · `recall_eligible` 후보 필터 · `adversary_false_reject` 분모 재정의 + `adversary_under_reject`·`neg_precedent_cited_rate`
- [ ] 근접 중복 클러스터(§4.3.2·§7.7) — `rr_registry.family_key` · 야간 ③-0 스캔 → `rr_curation_queue(kind='cluster_merge')` · `PUT /registry/{cluster}/merge`(422 `family_key_differs`·409 `alias_cycle`) · `cluster_dup_ratio` · `CurationQueue` 병합 화면. 자동 병합 없음
- [ ] 사람 finding UI·MCP·브리프 표기 — `TargetPage` '리스크 직접 등록' 폼(작성자만 수정·삭제) · MCP 7번째 도구 `risk_add_finding`(P3 REST 를 감쌈, `tools/list` 6→7) · E5 접두 2종 `[재검토]`·`[사람 제기·검증 대상]` · `risk_prior_include_human` 토글 · `registry.corpus_projects()` 를 E5·E6·E8·kNN 후보에 적용
- [ ] 다른 심의의 읽기 범위(§5.1 원칙 9·§6.11·§8.2.5) — `risk_get_registry`·`risk_claims_for_ref`·`risk_get_snapshot`·`risk_get_diff` 가 서비스 신원에는 `mcp_visibility='org'` 과제만, 개인 PAT 에는 멤버십 범위까지 열고 나머지는 `{error:'not_visible'}` · `hwax-risk-review.js` args 에 `briefToken` 필수(없으면 앱 호출 전 중단) · `TargetPage` '브리프 토큰 복사' 버튼 · ADH 회수 호출의 `required_tags`
- [ ] 통과 15항 — E5~E8 실림·드롭 0(최대 길이 픽스처) / kNN top1 / agent_search 재현 ≥0.8·E7 슬롯 규칙 / narr:/reg: 인용 ≥1·없는 인용 dangling / **계보 없는 과제 회수 ≥1** / sim-plan 좌석이 apps `heax-hwax_risk` 로 `risk_get_registry` 자유조회·`/tools-map` 수 P0 기준 불변 / MCP 워크플로 1패널 `risk_get_brief`→`risk_submit_panel_result` 원장 done·tier A `tier_a_web_only` / 판단어 0·부정 증거 항목 / MCP `actor` 미검증 기록·owner_sub 승계·미해석 `caller_unresolved`·헤더 미사용 0건 / **사람 finding 입구**(UI 폼·`risk_add_finding` 이 REST 와 바이트 동일 행·`tools/list` 7종·E5 접두·`risk_prior_include_human=false` 면 미탑재, (10)) / **코퍼스 필터**(`corpus_excluded=1` 과제가 E5·E6·E8·kNN 에서 0건, 풀면 복귀, 결측 문구의 n 도 필터 후 값, (11)) / **부정 선례**(기각 3건이 E5− 에만 실림·E5+ 0건·예산 700/300·항목 12·드롭 0·E7 상태 필터 3종 0줄·`exclude_tags` 인자·`status_retag` op·`neg_precedent_cited_n ≥1`·`adversary_false_reject` 분모 수동 계산 일치, (12)) / **근접 중복**(야간 큐 1행·자동 병합 0·병합 후 support=distinct 패널 수·다른 family 422·`cluster_dup_ratio` 감소·reject 한 쌍 재출현 0, (13)) / **필드·VOC 근거**(제품 연결 있는 타깃에서 E10 이 ≤5줄로 실리고 `voc:`·`paper:` 인용이 `GET /refs` 로 해석·미등록 타깃은 결측 문구 1줄+배지·조회 실패는 그 줄만 빠지고 브리프 조립은 완주·항목 수 12·드롭 0·rel 좌석의 `get_top_issues` 실호출 SSE ≥1·경로 4 가 product_code 없는 과제를 건너뜀, (14)) / **다른 심의 읽기 범위**(org 과제는 행 반환·private 과제는 `not_visible` 이고 그 결과가 브리프·발언에 실리지 않음, 개인 PAT 은 자기 멤버 과제까지 열림, `risk_get_brief` 는 `actor` 만으로는 안 열리고 `brief_token` 으로만 200, ADH 검색 인자에 `required_tags` 100%, (15))
- [ ] 결정(§10 8a·13·13a·17·26·31·38·40)
얻는 것 — 배치가 쌓일수록 브리프가 두터워지는 폐루프·다른 connectivity 회수·다른 심의의 등록부 읽기·Claude Code L2 보충 회차

## P6 학습 루프 (§9.7) — 전부 앱 리포
- [ ] `rr_labels/rr_patterns/rr_rules/rr_metrics/rr_curation_queue` 쓰기 경로 · 라벨 동기 잡(`sync_loop` 확장 — RA incident·test_run 게이트웨이 읽기·DynaForge 러너 자격·VOC·수동) · 조건 DSL·백테스트 · 지표 대시보드(TargetPage '품질' 카드)·'루프 작동' 배지 · `risk_pattern_card` doc_type(`bootstrap_adh.py`) · `visibility` 정책(§10 11) · RA typed 템플릿 절차서
- [ ] 통과 9항 — incident 동기 auto confirmed 는 match 1.0 만 / 라벨 20건 지표·n<5 표기 / 합성 30타깃 candidate 정확·거짓 0 / 승격 API 백테스트 게이트 422 / rule_hits 결정론 / taxonomy_version·재매핑 dry-run 0 / S2 게이트 전 예측기 코드 0줄 / org 토글 읽기·쓰기 0 / **택소노미 메이저 재매핑 전파**(별칭 `reason='taxonomy_major'` 행 생성·저장 cluster_key 불변·옛 `reg:` dangling 0·priors/patterns 재합산·`rr_patterns.merged_into`·승격 카운트에서 `rejected_in_panel`·`recall_eligible=0`·`weak_subject` 제외, (9))
- [ ] 결정(§10 11·12·21)
얻는 것 — 검증된 선례·규칙과 루프 작동 여부의 계측(앱 DB 누적, `GET /api/meta/metrics`)

## P7 ODB 실연동·예측기 승격(조건부) (§9.8)
- [ ] 앱 `adapters/ecad.py`(계약 4도구, `registry.py` 도구명 발견) · ir_version 1.1(`MIGRATIONS` v2) · refdes↔파트 사전 UI · `ecad.*` 이벤트 · SedInput 어댑터 · 예측기 HEAX 앱 계획서(별도 리포·매니페스트, 라벨 원천 `GET /api/export`)
- [ ] 통과 4항 — 계약·상한 테스트 / component↔mcad ≥0.9·deferred 재생성 / predict_sed out_of_range 강등 / 예측기 활성화 게이트(n_labeled ≥50·project ≥15)
- [ ] 선행(§10 9·14a): ODB hub 계약 합의, 명명 규칙

## cae00 이관(단계 아님 — §8.4.4 6, §10 21·22 결정 후, P3 통과 뒤)
- [ ] dev `build-all-to-drive.sh`(SIF·app-data → Drive) → cae00 `update.sh` 또는 `deploy-all-from-drive.sh`(`dist-from-drive.sh` 가 `var/sifs/hwax-risk.sif` 배치, 매니페스트는 HEAXHub git pull, `appdata-merge-from-drive.sh` 첫 배포 시드) → reconcile 기동 → cae00 에서 서비스 PAT·heax PAT·AIDH 키 재발급 → cae00 `secrets.env` → `redeploy-app.sh hwax-risk` → SettingsPage/TargetPage '재동기'. 이후 dev→cae00 은 `GET /api/export` → `POST /api/import` 만. dev 에서 `deploy-all` 류 금지

## 반영 절차(운영, 각 단계 후 — §8.4.4 1~5)
- [ ] 1 엔진: agent-server 재기동 → `sync-workflows.sh` → erag 재색인 → `check_chair_parity.py` exit 0
- [ ] 2 포털 창: `systems.yaml` 타일 → `POST /systems/reload` → 프론트 빌드·배포 → 포털 백엔드 재기동(`ConvCreate.kind`). 포털 `.env` 변경 없음
- [ ] 3 앱 등록·빌드(dev): `integrations/hwax-risk/.portal/manifest.yaml` 커밋 → 스캔 → `sif_build_hwax-risk.log` → reconcile 기동 → `integration_hwax_risk.log` → Caddy `/apps/hwax_risk/` 401 → 로그인 후 `/api/health` 200
- [ ] 4 app-data: `var/app_data/hwax_risk/` 확인 → `secrets.env`(0600) → `redeploy-app.sh hwax-risk` → `origin.json.hostname` 확인 → `bootstrap_ra_ontology.py --base <RA> --apply`(env `RA_ADMIN_PAT`) · `bootstrap_adh.py` → dev crontab `appdata-to-drive.sh` 일 1회
- [ ] 5 게이트웨이 자동 흡수 확인만(재시작 없음, 안 되면 매니페스트 status/mcp.expose → state 파일 → `heax_registry.token` 가시성 순으로 점검, restart 시 `-sTCP:LISTEN` 주의)
