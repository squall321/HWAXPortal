# 결손 하나 = 파일 하나 (PLAN §9-6)
id: stc-template-schema
title: 잡 템플릿의 계약이 산문이라 절차가 검증할 수 없다

flow: 해석 잡 제출 (R2 낙하·충격 등)
step: slurm_submit_job(template_id, files={...}) 의 **files 를 무엇으로 채우나**
kind: 2                       # ② 숨어 있음 — 2단인데 두 번째 단이 기계가 못 읽는다
owner_candidate: smart-twin-cluster

checked:
  - "2026-09-15 `slurm_list_templates` 실호출 — 템플릿 4개를 **텍스트 한 덩이**로 준다"
  - "필수 파일(file_key)·선택 파일이 산문에 적혀 있다. JSON Schema 가 없다"
  - "같은 자리를 DynaForge 는 `describe_operation` 이, SmartTwinMCP 는 `catalog_describe`
     가 **JSON Schema 로** 준다 — 한 앱만 산문이다"

evidence:
  - |
    slurm_list_templates 응답(실물, 발췌):
      • smarttwin-partial-impact  [simulation]  SmartTwin 전위치 부분충격
          필수 파일(file_key): model_k
          선택 파일: scenario_json

want: >-
  템플릿별 `args_schema`(또는 그에 준하는 구조). `describe_operation`·`catalog_describe`
  와 같은 모양이면 절차가 **저장 시점에** file_key 오타를 잡는다. 지금은 잡을 수 없고,
  잘못 적으면 잡을 던진 뒤에야 안다 — 잡 제출은 되돌리기 어려운 쪽이다.

status: open
confirmed_by: null
